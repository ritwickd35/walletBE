doctype
html(lang='en')
    head
        meta(charset='utf-8')
        title View Transactions
        script.
            const walletId = !{walletId};
        script(src=`/assets/js/transactionScript.js` defer)
    body
        